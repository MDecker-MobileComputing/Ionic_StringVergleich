import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.mide.ionic.stringvergleich',
  appName: 'StringVergleich',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
